# VeraProof AI - Centralized Port Configuration
# All services use ports in the 8xxx range to avoid conflicts

# Backend Services
BACKEND_HTTP_PORT=8100
BACKEND_HTTPS_PORT=8443

# Frontend Services
PARTNER_DASHBOARD_PORT=8200
VERIFICATION_INTERFACE_PORT=8300

# Infrastructure Services
POSTGRES_PORT=5432
LOCALSTACK_PORT=4566

# Derived URLs (for convenience)
BACKEND_HTTP_URL=http://localhost:8100
BACKEND_HTTPS_URL=https://localhost:8443
PARTNER_DASHBOARD_URL=http://localhost:8200
VERIFICATION_INTERFACE_URL=http://localhost:8300
